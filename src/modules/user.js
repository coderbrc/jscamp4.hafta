export default class User {
    constructor(id, firstName, lastName, city,age) {//construuctor kelime anlamı oluşturucu demektir. constructor yapıcı blok demek
        this.id = id
        this.firstName = firstName
        this.lastName = lastName
        this.city = city
        this.age=age
    }
}